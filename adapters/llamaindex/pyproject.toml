[project]
name = "remembr-llamaindex-adapter"
version = "0.1.0"
description = "LlamaIndex adapter for Remembr memory"
requires-python = ">=3.10"
dependencies = [
  "remembr>=0.1.0",
  "llama-index>=0.10.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
