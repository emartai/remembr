[project]
name = "remembr-langchain-adapter"
version = "0.1.0"
description = "LangChain adapter for Remembr memory"
requires-python = ">=3.10"
dependencies = [
  "remembr>=0.1.0",
  "langchain>=0.1.0",
]

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"
